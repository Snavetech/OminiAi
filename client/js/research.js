// ============================================================================
// Snave AI — Research Mode (client-side glue - main logic in chat.js)
// ============================================================================

const Research = (() => {
  function init() {
    // Research mode is handled via Chat.streamResearch
    // This module provides any additional research-specific UI
  }

  return { init };
})();
